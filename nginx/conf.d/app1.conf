server {
    listen 80;
    server_name app1.localhost;

    location / {
        proxy_pass http://app1:8001;
        proxy_set_header Host $host;
        proxy_set_header x-frorwarded-for $remote_addr;
        proxy_set_header x-frorwarded-proto $scheme;
    }
}